# --- Network Settings ---
HOST=0.0.0.0
PORT=8080
# The gRPC port used by the 'back' service to sync map data
GRPC_PORT=50053

# --- Security ---
# IMPORTANT: This must match the token in back/.env
MAP_STORAGE_API_TOKEN=123

# --- Services (Connecting back to the Backend) ---
# Used for authentication checks and metadata
API_URL=http://localhost:8081
PUSHER_URL=ws://localhost:8081

# --- Storage Settings ---
# 'public' means it looks in: map-storage/public/
S3_CHROOT=public
# Set to local to avoid looking for AWS/Minio
STORAGE_TYPE=local

# --- Logic ---
# Allows you to edit maps without strict permission checks locally
AUTHENTICATION_STRATEGY=none